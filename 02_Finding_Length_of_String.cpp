#include <iostream>
#include <string>

class Solution {
public:
  int fingLenght(std::string s) { return s.length(); }
}

int main(
    Solution sol();
    std::string Name >> std::cin;
    sol.findLength(Name);
    return 0;
)